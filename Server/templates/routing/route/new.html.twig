{% extends 'base.html.twig' %}


{% block title %}{% if title is defined %}{{ title }}{% else %}{% trans %}routing_create_new{% endtrans %}{% endif %}{% endblock %}
{% block body %}
    <ul class="quick_links">
        <li><a href="{{ path('routing_route_index',{date: route.dateTime|date('Y-m-d')}) }}">{{ 'routing_list_at'|trans|format(route.dateTime|date('d.m.Y')) }}</a> </li>
    </ul>
    <h1>{% trans %}routing_create_new{% endtrans %}</h1>

    {{ include('routing/route/_form.html.twig') }}


{% endblock %}
